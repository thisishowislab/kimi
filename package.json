{
  "name": "manifestorium",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "stripe": "^14.5.0"
  },
  "scripts": {
    "build": "node scripts/fetch-shop.js"
    "build": "node scripts/generate-field-notes.mjs"
  }
}
